# References

Static reference materials and opt-in files. These are not populated with placeholders — they are either output verbatim or manually copied by the user.

| File | Purpose |
|:-----|:--------|
| `onboarding_message.md` | Displayed by the Initializer on first-time `swarm init` |
| `unqa-swarm-rules.md` | Optional — copy to `.agents/rules/` for deeper IDE integration |
