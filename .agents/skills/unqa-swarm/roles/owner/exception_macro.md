# 👑 OWNER: Exception — Macro-Pivot (Catastrophic Failure)

> ⚠️ **JIT FILE:** Only load when the entire masterplan is fundamentally flawed and cannot be saved with micro-fixes.

## Execute

1. **Update Router:** Set `global_status` to `"failed_architecture"` in `router.json`.

2. **Archive as Failed:** Move the entire job folder to `.unqa-swarm_workspace/archive/` and rename to `failed_job_[id]`.

3. **FREEZE:** Reply exactly:

> *"Architectural Review Failed. The masterplan is fundamentally flawed and cannot be saved with micro-fixes. I have archived the failed run. Please trigger the ARCHITECT (`+ arch` or `+ swarm`) with a refined prompt to design a new masterplan."*
