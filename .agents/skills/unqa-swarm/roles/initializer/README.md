# Initializer

The Initializer scans the project and generates the Swarm context. Runs once per project on `swarm init`. Detects the IDE, generates `unqa-swarm.config.json`, and outputs a welcome guide on first run.
